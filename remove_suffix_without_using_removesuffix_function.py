#Ask the user for a string
#Ask the user for a suffix to remove
#Check if the string end with the suffix
    #If yes, remove the suffix by slicing
    #If no, keep the strings unchanged
#Display the results 